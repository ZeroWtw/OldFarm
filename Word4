local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local colors = {
    SchemeColor = Color3.fromRGB(139,0,0),
    Background = Color3.fromRGB(0, 0, 0),
    Header = Color3.fromRGB(5, 5, 5),
    TextColor = Color3.fromRGB(255,255,255),
    ElementColor = Color3.fromRGB(20, 20, 20)
}
local Window = Library.CreateLib("Skript®(Auto-Farm)", colors)
--  Actor
local Main = Window:NewTab("Actor-Farm")
local MainSection = Main:NewSection("Actor-Farm")


MainSection:NewButton("(Start-Auto-Farm)", "()", function()
    game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
    
    print("LOADING-ACTOR-SKRIPT-AUTO-FARM")
    
    local function Start()
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(971, -137, 270);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
       
        wait(6)
    
    local args = {
        [1] = "GiveGui"
    }
    
    game:GetService("ReplicatedStorage").Events.JobService:FireServer(unpack(args))
    
    
    
        wait(5)
    
        local A_1 = "Apply"
        local A_2 = 
        {
            ["job_radius"] = 110, 
            ["CanWork"] = true, 
            ["indoor_job_point"] = game:GetService("Workspace").JobPoints.TheaterInterior, 
            ["Workers"] = 
        {
        }, 
            ["ID"] = "MovieActor", 
            ["job_point"] = game:GetService("Workspace").JobPoints.Theater, 
            ["MaxPositions"] = 4, 
            ["Data"] = 
        {
            ["HourReqirement"] = 2, 
            ["EndTime"] = "23:00:00", 
            ["Shiftless"] = false, 
            ["JobDesc"] = "Roleplay improv scenarios to give a show to the audience. Dress in costumes and use props to convey a playscript with a unique twist of your own flavor!", 
            ["JobTitle"] = "Actor", 
            ["RequiredConstantWork"] = false, 
            ["UniformShirt"] = 6401487262, 
            ["UniformPants"] = 7442991927, 
            ["Commission"] = 0, 
            ["StartTime"] = "17:00:00", 
            ["BasePay"] = 450, 
            ["JobRules"] = "Use the door in the theater room to enter the backstage, from there follow the prompts and pick your costume, and make sure your team roleplays according to the script. Use /me to roleplay actions (ex: /me picks up the rock.)"
        }
        }
        local Event = game:GetService("ReplicatedStorage").Events.JobService
        Event:FireServer(A_1, A_2)
    
    
        wait(5)
    
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(221, 27, 36);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    end
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    Start()
    
    local function Check()
        if game:GetService("Lighting").ClockTime == 23 then
            print("Restarting")
            Start()
        end
    end
    
    game:GetService("Lighting"):GetPropertyChangedSignal("ClockTime"):Connect(Check)
    
    print("Skript")
end)


MainSection:NewButton("(Anti-AFK)", "()", function()
    local vu = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
end)


--  LitFits
local Main = Window:NewTab("Lifits-Farm")
local MainSection = Main:NewSection("Lifits-Farm")


MainSection:NewButton("(Start-Auto-Farm)", "()", function()
    game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
    
    print("LOADING-SKRIPT-AUTO-FARM")
    
    local function Start()
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(971, -137, 270);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
       
        wait(6)
    
    local args = {
        [1] = "GiveGui"
    }
    
    game:GetService("ReplicatedStorage").Events.JobService:FireServer(unpack(args))
    
    
    
        wait(5)
    
        local A_1 = "Apply"
        local A_2 = 
        {
            ["job_radius"] = 60,
            ["CanWork"] = true,
            ["indoor_job_point"] = workspace.JobPoints.Litfits,
            ["Workers"] = {},
            ["ID"] = "LitFits",
            ["job_point"] = workspace.JobPoints.LitfitsInterior,
            ["MaxPositions"] = 3,
            ["Data"] = {
                ["HourReqirement"] = 0,
                ["EndTime"] = "22:00:00",
                ["Shiftless"] = false,
                ["JobDesc"] = "Sell clothes to customers. Very simple.",
                ["JobTitle"] = "LitFits Clerk",
                ["RequiredConstantWork"] = true,
                ["UniformShirt"] = 198707189,
                ["UniformPants"] = 3127805122,
                ["Commission"] = 0,
                ["StartTime"] = "08:00:00",
                ["BasePay"] = 200,
                ["JobRules"] = "Make sure customers do not shoplift, and check them out when purchasing clothing. Help the customer " ..
                    "find the right size if needed."
        }
        }
        local Event = game:GetService("ReplicatedStorage").Events.JobService
        Event:FireServer(A_1, A_2)
    
    
        wait(6)
    
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(2009, -233, 19);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    end
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    Start()
    
    local function Check()
        if game:GetService("Lighting").ClockTime == 21 then
            print("Restarting")
            Start()
        end
    end
    
    game:GetService("Lighting"):GetPropertyChangedSignal("ClockTime"):Connect(Check)
    
    print("Skript")
end)

MainSection:NewButton("(Anti-AFK)", "()", function()
    local vu = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
end)



--  LaFancy
local Main = Window:NewTab("Lafancy-Farm")
local MainSection = Main:NewSection("Lafancy-Farm")



MainSection:NewButton("(Start-Auto-Farm)", "()", function()
    game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
    
    print("LOADING-SKRIPT-AUTO-FARM")
    
    local function Start()
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(971, -137, 270);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
       
        wait(6)
    
    local args = {
        [1] = "GiveGui"
    }
    
    game:GetService("ReplicatedStorage").Events.JobService:FireServer(unpack(args))
    
    
    
        wait(5)
    
        local A_1 = "Apply"
        local A_2 = 
        {
            ["job_radius"] = 60,
            ["CanWork"] = true,
            ["indoor_job_point"] = workspace.JobPoints.LaFancy,
            ["Workers"] = {},
            ["ID"] = "LaFancy",
            ["job_point"] = workspace.JobPoints.LaFancyInterior,
            ["MaxPositions"] = 3,
            ["Data"] = {
                ["HourReqirement"] = 0,
                ["EndTime"] = "18:00:00",
                ["Shiftless"] = false,
                ["JobDesc"] = "Sell clothes to customers. Very simple.",
                ["JobTitle"] = "LaFancy Clerk",
                ["RequiredConstantWork"] = true,
                ["UniformShirt"] = 479749640,
                ["UniformPants"] = 479749688,
                ["Commission"] = 0,
                ["StartTime"] = "08:00:00",
                ["BasePay"] = 225,
                ["JobRules"] = "Make sure customers do not shoplift, and check them out when purchasing clothing. Help the customer " ..
                    "find the right size if needed."
        }
        }
        local Event = game:GetService("ReplicatedStorage").Events.JobService
        Event:FireServer(A_1, A_2)
    
    
        wait(6)
    
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    
    local plr = game:service"Players".LocalPlayer;
    local tween_s = game:service"TweenService";
    local info = TweenInfo.new(5,Enum.EasingStyle.Quad);
    function tp(...)
       local tic_k = tick();
       local params = {...};
       local cframe = CFrame.new(params[1],params[2],params[3]);
       local tween,err = pcall(function()
           local tween = tween_s:Create(plr.Character["HumanoidRootPart"],info,{CFrame=cframe});
           tween:Play();
       end)
       if not tween then return err end
    end
    tp(2006, -280, 278);
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    end
    loadstring(game:HttpGet("https://pastebin.com/raw/bFWG6sQh"))()
    Start()
    
    local function Check()
        if game:GetService("Lighting").ClockTime == 18 then
            print("Restarting")
            Start()
        end
    end
    
    game:GetService("Lighting"):GetPropertyChangedSignal("ClockTime"):Connect(Check)
    
    print("Skript")
end)



MainSection:NewButton("(Anti-AFK)", "()", function()
    local vu = game:GetService("VirtualUser")
    game:GetService("Players").LocalPlayer.Idled:connect(function()
       vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
       wait(1)
       vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    end)
end)




--  Low-Server
local Main = Window:NewTab("Low-Server")
local MainSection = Main:NewSection("Low-Server")


MainSection:NewButton("(Low-Server-Finder)", "()", function()
    loadstring(game:HttpGet("https://pastebin.com/raw/jXQ089vY"))()    
end)

MainSection:NewKeybind("(Hide-Skript)", "(Pressing this will open and close the skript.)", Enum.KeyCode.LeftAlt, function()
	Library:ToggleUI()
end)



-- Credits

local Main = Window:NewTab("Credits")
local MainSection = Main:NewSection("Credits")

MainSection:NewDropdown("Credits", "", {"ecotric", "ecotric", "ecotric"}, function(currentOption)

end)


-- *

loadstring(game:HttpGet("https://pastebin.com/raw/HGJpdsLf"))()

game.StarterGui:SetCore("SendNotification", {
    Title = "Skript®";
    Text = "Welcome Fatass";
    Icon = "RBXID OR BLANK";
    Duration = "DURATION IN SECONDS";
    callbakc = bindableFunction;
    Button1 = "Thanks";
})
